#!/usr/bin/env bash
# Stop the Home Energy Manager daemon (if running).
#
# Usage: ./bin/stop
#
set -e
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
export PYTHONPATH="$ROOT"
# shellcheck source=bin/lib.sh
. "$(dirname "$0")/lib.sh"
exec "$(hem_python "$ROOT")" -m src.cli daemon stop "$@"
