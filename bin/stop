#!/usr/bin/env bash
# Stop the Home Energy Manager daemon (if running).
#
# Usage: ./bin/stop
#
set -e
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
export PYTHONPATH="$ROOT"
exec "$ROOT/.venv/bin/python" -m src.cli daemon stop "$@"
