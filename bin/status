#!/usr/bin/env bash
# Show Home Energy Manager daemon status (running or not, PID, API and OpenClaw URLs).
#
# Usage: ./bin/status
#
set -e
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
export PYTHONPATH="$ROOT"
exec "$ROOT/.venv/bin/python" -m src.cli daemon status "$@"
